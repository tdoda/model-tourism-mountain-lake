# Note: name can be changed
name: "lake-tourism"
channels:
	# important: do not use 'defaults', but always include 'nodedefaults'
  - conda-forge
  - nodefaults
dependencies:
  - python=3.11 # set your python version here
  - netCDF4
  - numpy
  - pandas
  - xarray
  - matplotlib
  - cmocean
  #- ipympl
  # - pip:
	  # put packages to be installed by pip here
    # - ...
# Note: prefix can be changed
prefix: "/opt/conda"

